#metadata((
  title: "概率的概率",
  published: "2026-09-20",
  description: "当概率本身成为随机变量，概率的概率该如何去衡量？",
  tags: ("数学","概率","贝叶斯推断"),
  category: "数学研究",
  draft: false,
))<frontmatter>

#html.elem("p", attrs: (style: "margin-top: 2em;")) 

想不到吧，还是这个机器！同一情境，却是完全不同的问题！

> 前置知识：使用概率密度函数描述连续型随机变量，换元积分法与分部积分法。（推荐学习）

=== 问题的提出

机器不断地随机输出由 0\~9 组成的数字。我们只知道机器内置了每次输出这 10 个数字的固定概率，而并不知道它们具体等于多少。我们只能观测到机器的一段输出，然后试图通过分析输出序列来获得有关机器内置概率的信息。那么我们该如何尽可能地对一段机器的输出序列进行分析呢？

- 首先对这个问题进行数学建模：假定机器内置输出 0\~9 的概率分别为 $P_0 ~ P_9$（未知），我们观测到的序列长为 $N$，其中出现 0\~9 的次数分别为 $n_0 ~ n_9$（已知）。
  - 注意，这里我们只关心出现 0\~9 的次数，而并不关心它们出现的顺序。也就是说，我们从一段给定的输出序列中只提取有效信息，而忽略对我们无效的信息。这么做的合理性，即为什么只有 0\~9 的出现次数为有效信息，请读者自行想明。
- 最朴素的想法自然是用频率估计概率，即估计 $P_i = n_i/N$。然而我们仅仅只能做到这一步吗？这里我们只给出了一个简单的估计，却没有提供任何其它关于机器内置概率的信息，比如：这样的估计有多可靠？机器的概率就没有可能是其它的值吗？如果可能，那么可能性又有多大？……如此想来，简单地用频率估计概率是远远不够的，我们期待从机器的输出序列中得到更多对内置概率的分析结果。
- 这里，一个极为关键的思想，是把 $P_0 ~ P_9$ 看成随机变量，而不只是简单地把它们视作未知的常数。我们要的不仅是对它们的一个简单的估计值，而是对它们具体可能取什么值、有多可能的详细描述。简言之，我们其实可以试图求出它们的分布。

=== 视角的转换：把概率本身视为随机变量

这里其实挺有趣的，需要一个思想的转换。我们往往把机器的内置概率当成“给定常数”，把输出结果当成需要求解的“随机事件”。但在这里，当“给定常数”变为未知，“随机事件”的结果却成为已知时，我们要转变思路，把所谓的随机输出结果看成条件，而把看似确定、实则在我们认知中不定的内置概率视作一个待求解的随机变量。

- 首次明确提出这一思想的是英国数学家贝叶斯（Thomas Bayes，1702-1761），他在 1763 年的遗作中给出了这一原理的最初形式；法国数学家、天文学家拉普拉斯（Pierre-Simon Laplace，1749-1827）独立地发现了它，并把问题推广到连续参数的情形，系统发展为“逆概率”理论。拉普拉斯认为，概率不仅仅是描述事件发生的频率，更是我们对未知事物认知状态的量化。
- 这一视角下最核心的概念是*似然*（likelihood）。百度百科对它的说明是：似然函数在推断统计学（Statistical inference）中扮演重要角色，尤其是在参数估计方法中。在教科书中，似然常常被用作“概率”的同义词，但是在统计学中，二者有截然不同的用法：概率描述了已知参数时的随机变量的输出结果；似然则用来描述已知随机变量输出结果时，未知参数的可能取值。例如，对于“一枚正反对称的硬币上抛十次”这种事件，我们可以问硬币落地时十次都是正面向上的“概率”是多少；而对于“一枚硬币上抛十次”，我们则可以问，这枚硬币正反面对称的“似然”程度是多少。
  - 注意：似然是个条件概率。
- 进一步思考：概率是对随机事件抽象的全知建模，参数已知，由因推果；而统计则从未知出发，区分表象与本质，将认识世界的过程数学抽象化，探寻从数据定性理解内在似然的方法，由果溯因。二者恰似一枚硬币的两面，而本文所做的，正是从其中一面走到另一面。
  // 我在想是不是可以深入了解一下概率的数学史。

=== 最简情形：只输出 0 和 1

我们先从最简单的情况开始研究。不妨假定机器只输出 0 和 1，输出 0 的概率为随机变量 $P$，而输出 1 的概率为 $1-P$。已知序列中出现 0、1 的次数分别为 $m$、$n$，求 $P$ 的分布。

- 这是贝叶斯公式的典型应用场景。设事件 $A$ 为“序列中出现 0、1 的次数分别为 $m$、$n$”。记 $P$ 的先验密度为 $f_0 (p)$，则根据贝叶斯公式，$P$ 的后验密度为

  $f_P (p) = (f_0 (p) p^m (1-p)^n)/(integral_0^1 f_0 (p) p^m (1-p)^n d p)$

  这里有一个连续情形特有的疑点需要讲清楚：贝叶斯公式在离散情形下是求和，到了连续情形却变成了积分，我们能自动地把贝叶斯公式应用于连续场景吗？答案是肯定的——只要把求和换成积分、把分布列换成概率密度即可，上面的式子就是这个“翻译”的结果。
- 其实仔细想来，这里还有一个先验分布的问题值得考虑。当我们未获取任何有关的信息时，我们只能假定它是一个均匀分布。均匀是分布的默认。

  取 $f_0 (p) = 1$，于是

  $f_P (p) = (p^m (1-p)^n)/(integral_0^1 p^m (1-p)^n d p)$

  接下来的问题是，分母的积分可以求解吗？

  // 最棒的方法：实际教学试验！看看实际教学中学生是怎么想的，才能更好地书写教学！

- 分析与推导：记 $I_(m, n) = integral_0^1 p^m (1-p)^n d p$。

  先看微分：

  $d(p^m (1-p)^n) = m p^(m-1) (1-p)^n d p - n p^m (1-p)^(n-1) d p$

  两边同除以 $m! n!$，凑成左右同型：

  $d((p^m (1-p)^n)/(m! n!)) = (p^(m-1) (1-p)^n)/((m-1)! n!) d p - (p^m (1-p)^(n-1))/(m! (n-1)!) d p$

  两边同时对 $p$ 从 0 到 1 积分。左边是 $(p^m (1-p)^n)/(m! n!)$ 在两端的取值之差，而它在 $p = 0$ 与 $p = 1$ 处均为 0，故左边为 0；于是右边两项相等：

  $(I_(m-1, n))/((m-1)! n!) = (I_(m, n-1))/(m! (n-1)!)$

  整理得 $I_(m, n) = m/(n+1) dot I_(m-1, n+1)$。这个递推每次把 $m$ 减 1、$n$ 加 1，两者之和不变，反复使用直到 $m = 0$：

  $I_(m, n) = m/(n+1) dot (m-1)/(n+2) dot dots dot 1/(n+m) dot I_(0, n+m)$

  而 $I_(0, n+m) = integral_0^1 (1-p)^(n+m) d p = 1/(n+m+1)$，再注意到 $(n+1)(n+2) dots (n+m) = (m+n)!/n!$，于是

  $I_(m, n) = (m! n!)/(m+n+1)!$

  这就得到了我们想要的结果：

  $integral_0^1 p^m (1-p)^n d p = (m! n!)/(m+n+1)!$

  代回后验密度的表达式，便得到

  $f_P (p) = ((m+n+1)!)/(m! n!) p^m (1-p)^n$

  至此，$P$ 的分布便被完全刻画出来了。

=== 预测下一个输出：继承法则

求出了 $P$ 的分布，事情并没有结束——恰恰相反，这才刚刚开始。因为我们真正关心的往往不是 $P$ 究竟等于多少，而是要拿这个分布去预测机器的下一次输出。

假设机器再输出一次，这一次输出 0 的概率是多少？

- 由于 $P$ 本身就是随机的，这个概率应当是对 $P$ 取平均的结果，也就是后验均值：

  $E(P) = integral_0^1 p dot f_P (p) d p = (integral_0^1 p^(m+1) (1-p)^n d p)/(integral_0^1 p^m (1-p)^n d p)$

  代入上一节的积分公式：

  $E(P) = ((m+1)! n!)/(m+n+2)! dot (m+n+1)!/(m! n!) = (m+1)/(m+n+2)$

  也就是说，预测下一个输出为 0 的概率是 $(m+1)/(m+n+2)$。对比最初那个朴素的频率估计 $m/(m+n)$，分子分母各多了一个 1，这就是“均值 +1 的奇妙修正”。
  - 联想：对一遵循特定分布的随机变量 $X$ 进行 $n$ 次观测，则观测样本数据的方差 $S^2$ 满足 $E(S^2) = (n-1)/n dot D(X)$。同样是朴素估计需要修正的情形。
- 这个小小的修正，正是拉普拉斯那个著名问题的答案。考虑一个每天是否升起的太阳，已经连续升起了 $N$ 天，一天也没有落下过。用本文的语言，这就是 $m = N$、$n = 0$ 的情形，代入得

  $E(P) = (N+1)/(N+2)$

  - 当 $N = 0$ 时，答案是 $1/2$——对太阳是否升起一无所知，只能给出一半对一半。
  - 随着 $N$ 增大，这个概率不断逼近 1，但永远达不到 1：“明天不升起”的概率始终是 $1/(N+2)$。若太阳已经升起了 5000 年，约 $1.8 times 10^6$ 天，那么它明天不升起的概率约为 $1/(1.8 times 10^6)$，小得可以忽略，但严格大于零。
  - 无条件地相信与绝对的把握之间，永远留着一条缝。这条缝，就是归纳法的全部难处所在。

  这个结果被称为*继承法则*（rule of succession），由拉普拉斯在 1814 年出版的《概率的哲学探讨》中给出。

=== 贝叶斯更新与贝塔分布

- 假如信息进一步更新，我们可以：\
  ① 把现有信息得出的概率密度函数作为新的先验，再求一次后验分布；\
  ② 直接把现有信息与新信息结合起来，求出新的后验分布。\
  这两种方法的结果是一样的！这合理而神奇。这一过程被称为*贝叶斯更新*。
- 事实上，我们可以用几个参数表示这类特定的分布，以简化这一过程。现代数学中，定义*贝塔分布* $B e(alpha, beta)$：

  $f_(X ~ B e(alpha, beta))(x) = (x^(alpha-1) (1-x)^(beta-1))/(B(alpha, beta))$

  其中 $B(alpha, beta) = integral_0^1 x^(alpha-1) (1-x)^(beta-1) d x$，即上一节算出的那类积分，称为*贝塔函数*，又称第一类欧拉积分。

  未知信息时的先验分布，即均匀分布，为 $B e(1, 1)$。若 $P$ 的先验分布为 $B e(alpha, beta)$，新信息中序列出现 0、1 的次数分别为 $m$、$n$，则 $P$ 的后验分布为

  $B e(alpha + m, beta + n)$

- 进行这一步抽象后，一切是如此的简洁！获取新信息后，我们只需要简单地用新信息做加法更新两个参数，就能获得新的后验分布。（这种时候，你也能逐渐意识到这些函数的现代定义中有时会多加个 1 或减个 1 的原因，一切都是为了大局！最终的形式简洁，一切定义都是最优化的。）
- 现代数学中，若先验分布与后验分布属于同一类分布，那么它们就被称为*共轭分布*，而该先验分布就是该似然函数的*共轭先验*（在这里，贝塔分布就是二项分布的共轭先验）。通过不断地更新这个分布，我们可以不断地完善对未知概率的认知，从而更好地理解和预测这个机器的行为。

=== 拓展到多项分布

现在我们把问题拓展回最初的情景。

- 二项分布 $->$ 多项分布，贝塔函数 $->$ 狄利克雷积分（区分于狄利克雷函数，那不是概率论中的，而是数学分析中的），贝塔分布 $->$ 狄利克雷分布（又称多元贝塔分布）。这一路的推广是自然的：把 0 和 1 两个结果拓展成 $k$ 个结果，把二值概率 $P$ 拓展成概率向量 $(P_1, dots, P_k)$，约束条件由 $P + (1-P) = 1$ 变为 $sum_i P_i = 1$。
- 狄利克雷积分是贝塔函数的直接推广，把贝塔函数中的 $(1-x)^(beta-1)$ 一分为二、逐个换元，再作归纳即可：

  $integral_(sum_i x_i <= 1, x_i >= 0) x_1^(alpha_1 - 1) dots x_k^(alpha_k - 1) d x_1 dots d x_k = (Gamma(alpha_1) dots Gamma(alpha_k))/(Gamma(alpha_1 + dots + alpha_k + 1))$

  其中 $Gamma$ 是伽马函数。$k = 1$ 时它退化为 $integral_0^1 x^(alpha-1) d x = 1/alpha$，$k = 2$ 时即为贝塔函数。
- 相应地，狄利克雷分布 $D i r(alpha_1, dots, alpha_k)$ 的密度为

  $f(p_1, dots, p_k) = (p_1^(alpha_1 - 1) dots p_k^(alpha_k - 1))/(B(alpha_1, dots, alpha_k))$

  其中归一化常数 $B(alpha_1, dots, alpha_k) = (Gamma(alpha_1) dots Gamma(alpha_k))/(Gamma(alpha_1 + dots + alpha_k))$。它同样是多项分布的共轭先验：若先验为 $D i r(alpha_1, dots, alpha_k)$，观测到各结果出现次数 $n_1, dots, n_k$，则后验为 $D i r(alpha_1 + n_1, dots, alpha_k + n_k)$。贝叶斯更新的加法规则，在这里一字不改地成立。

  // 这有点复杂，要写清楚。

- 顺带一提，这套公式在自然语言处理中有一个更广为人知的化身。语言模型预测“下一个字符是什么”时，用的正是多项分布的贝叶斯更新，只不过通常直接取 $alpha_i = 1$ 的狄利克雷先验，此时后验均值为 $(n_i + 1)/(N + k)$：把每个字符的频数都加上 1 再归一化。这个操作在 NLP 教材里叫作*加一平滑*（add-one smoothing），又称拉普拉斯平滑。
- 它能这样直接对应，是因为语言模型面对的困境和本文的机器一模一样：训练语料里没出现过的字符，用频率估计会得到概率 0，而取对数后就是负无穷。加一平滑的实质，是预先给每个字符记上一次虚拟的出现次数，从而保证没有任何字符的概率为 0。
- 那么，为什么加的是 1 而不是别的数呢？这就要回到本文最后的问题：先验分布究竟该选什么？

=== 先验分布究竟是什么

- 其中有一个很隐蔽但很关键的小问题需要被解决：多项情形下的先验分布是什么？即把 1 的概率以最平均的方式分给 $k$ 个数，其中每个数的概率的概率分布是什么？
  - 本质上是计算边缘分布。
  - 等效的：把长度为 1 的线段随机分成 $k$ 份，其中一份长度的概率分布？
  - 本质：把 1 分成 $k$ 份，一份大小的概率分布？
- 怎么分？默认是均匀随机。何谓均匀随机？本质是联合概率密度函数均匀（定值）。这是最朴素的随机分割方式。在离散情况中就对应古典概型，即每种情况的概率相等；连续情况下每个点的概率都为 0，则是每种情况的概率密度相等。
- 具体怎么求这种均匀联合分布的边缘分布？有几种思维方式：

  设 $(P_1, dots, P_k)$ 在单纯形 $\{p_i >= 0, thin sum_i p_i = 1\}$ 上均匀分布，求 $P_1$ 的边缘分布。

  - 方法一：直接积分。给定 $P_1 = p_1$，剩下的 $(P_2, dots, P_k)$ 落在 $(k-2)$ 维单纯形 $\{sum_(i >= 2) p_i = 1 - p_1, thin p_i >= 0\}$ 上，而这样一个“边长”为 $1 - p_1$ 的 $(k-2)$ 维单纯形，其体积正比于 $(1-p_1)^(k-2)$。均匀分布下，边缘密度正比于剩余区域的体积，故

    $f_(P_1)(p_1) prop (1-p_1)^(k-2)$

    由归一化条件 $integral_0^1 (1-p_1)^(k-2) d p_1 = 1/(k-1)$ 定出系数，得

    $f_(P_1)(p_1) = (k-1)(1-p_1)^(k-2)$

    即 $P_1 ~ B e(1, k-1)$。检验：$k = 2$ 时它退化为 $f_(P_1)(p_1) = 1$，正是均匀分布，与二值情形自洽。
  - 方法二：用伽马分布构造。设 $X_1, dots, X_k$ 相互独立，均服从参数为 1 的伽马分布（即标准指数分布），令 $P_i = X_i/(sum_j X_j)$，则 $(P_1, dots, P_k)$ 恰好服从单纯形上的均匀分布。这一构造把“带约束的均匀”化归成了“无约束的独立”：分母是 $k$ 个独立伽马变量之和，它自身服从参数为 $k$ 的伽马分布；而若干个相互独立的伽马变量之比服从贝塔分布，于是立刻得到 $P_1 ~ B e(1, k-1)$，与方法一殊途同归。

- 对称的分法只有这一种吗？如何证明？

  均匀是一种自然的选择，但它并非唯一，甚至并不像看上去那么“中立”。真正的问题在于：所谓“均匀”，依赖于我们拿什么参数来衡量。

  以二值情形为例。说 $P$ 在 $[0, 1]$ 上均匀，等价于说“机器输出 0 的概率”没有偏好。但换一个参数，比如用几率比 $r = p/(1-p)$ 来描述这台机器，$r$ 的取值范围是 $[0, +oo)$，而它的密度是 $1/(1+r)^2$，并不是均匀的。同一台机器，同一个“没有偏好”的信念，换一种描述方式就给出了不同的先验。均匀先验并不像它看起来那样中立。

  若要求先验对参数化的选择免疫——无论用什么参数描述这台机器，得到的先验都是“同一个”——那么解是唯一的：

  $f_0 (p) prop sqrt(det I(p))$

  其中 $I(p)$ 是*费舍尔信息量*（Fisher information）。这就是*杰弗里斯先验*（Jeffreys prior）。

  对二值情形，单次观测的费舍尔信息为

  $I(p) = 1/(p(1-p))$

  于是 $f_0 (p) prop 1/sqrt(p(1-p))$，即 $B e(1/2, 1/2)$；推广到 $k$ 元，就是 $D i r(1/2, dots, 1/2)$。与均匀先验 $B e(1, 1)$ 相比，它在两端发散、中间凹陷，反而更偏好极端的取值——无信息并不等于均匀，这多少有些反直觉。

  两种先验给出的后验均值也不同：

  - 均匀先验：$(n_i + 1)/(N + k)$，这正是前文提到的加一平滑；
  - 杰弗里斯先验：$(n_i + 1/2)/(N + k/2)$，对应 $alpha = 1/2$ 的平滑。

  在通用编码的意义下，后者恰好是最优的：Krichevsky 与 Trofimov 在 1981 年证明，$alpha = 1/2$ 的估计量的累积冗余有上界 $(k-1)/2 dot log N + O(1)$，且这个界是最优的，没有任何估计量能对所有序列都做得更好。$alpha = 1$ 的冗余同样是对数阶的，只是常数项更差——它胜在算术简单，而不是理论地位。

  所以，均匀是分布的默认，却未必是最好的默认。

=== 结语

其实现实就是这样。我们往往只能看到一个事物的表象，而这是我们去推断其内在的唯一根据。好在这个推断不是一次性的，它可以通过贝叶斯更新不断迭代、不断修正。

这一简单的问题，与我们认识世界其实本质上是一样的。一个输出随机序列的机器，成为了我们认识世界的一个极简却深刻的隐喻。

这便是概率的概率。
